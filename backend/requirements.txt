# Runtime dependencies — only what backend/ actually imports.
# (The original list came from the Emergent base image and pulled in ~100
# unused packages plus platform-private wheels; kept at requirements.emergent.txt.bak)

fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
python-multipart==0.0.20

httpx==0.28.1

pydantic==2.13.4
python-dotenv==1.2.1

resend==2.40.2
