User-agent: *
Allow: /

# The owner's CMS. Nothing here is useful to a search engine, and it should not
# be advertised. This is not access control — /studio is protected by its own
# password, not by this file.
# Studio uses an HTTP X-Robots-Tag: noindex header; allow crawling to see it.
# Public API responses stay crawlable so Google can render the React pages.

Sitemap: https://www.kodarchive.ink/sitemap.xml
